<Window x:Class="RSA.Guest.EmailPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create profile"
        Height="190"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#DAE6E6">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Enter your email before creating a profile:" Margin="0,0,0,8" Foreground="#101D2F" FontSize="14" />

        <TextBox x:Name="EmailTextBox"
                 Grid.Row="1"
                 Height="34"
                 Padding="8,6"
                 VerticalContentAlignment="Center" />

        <TextBlock x:Name="ErrorText"
                   Grid.Row="2"
                   Margin="0,8,0,0"
                   Foreground="#B3261E"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="88" Height="34" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="OK" Width="88" Height="34" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
