# Commandes Git/GitHub Essentielles

## Configuration initiale
git config --global user.name "Votre Nom"
git config --global user.email "votre@email.com"

## État et information
git status                          # Voir l'état des fichiers
git log                            # Historique des commits
git log --oneline                  # Historique condensé
git branch -a                      # Lister toutes les branches (locales et distantes)
git remote -v                      # Voir les dépôts distants

## Commits
git add .                          # Ajouter tous les fichiers modifiés
git add fichier.cs                 # Ajouter un fichier spécifique
git commit -m "Message de commit"  # Créer un commit
git commit -am "Message"           # Add + commit (fichiers déjà trackés)

## Branches - Créer et changer
git branch nom-branche             # Créer une nouvelle branche
git checkout nom-branche           # Changer de branche
git checkout -b nom-branche        # Créer et changer de branche
git switch nom-branche             # Alternative moderne pour changer de branche
git switch -c nom-branche          # Créer et changer (moderne)

## Branches - Fusionner
git checkout main                  # Se mettre sur la branche de destination
git merge nom-branche              # Fusionner nom-branche dans main

## Branches - Supprimer
git branch -d nom-branche          # Supprimer branche locale (sécurisé)
git branch -D nom-branche          # Forcer suppression branche locale
git push origin --delete nom-branche  # Supprimer branche distante

## Push/Pull
git pull origin main               # Récupérer les changements distants
git push origin main               # Envoyer les commits locaux
git push origin nom-branche        # Pousser une branche spécifique
git push -u origin nom-branche     # Pousser et lier la branche

## Annuler des changements
git restore fichier.cs             # Annuler modifications non committées
git restore --staged fichier.cs    # Retirer du staging
git reset HEAD~1                   # Annuler le dernier commit (garde les changements)
git reset --hard HEAD~1            # Annuler le dernier commit (perd les changements)

## Stash (mettre de côté)
git stash                          # Sauvegarder temporairement les changements
git stash list                     # Lister les stash
git stash pop                      # Récupérer le dernier stash
git stash apply                    # Appliquer le stash sans le supprimer

## Différences
git diff                           # Voir les modifications non stagées
git diff --staged                  # Voir les modifications stagées
git diff branche1 branche2         # Comparer deux branches

## Workflow typique
# 1. Créer une branche de fonctionnalité
git checkout -b nouvelle-feature

# 2. Travailler et committer
git add .
git commit -m "Ajout de la fonctionnalité X"

# 3. Pousser la branche
git push -u origin nouvelle-feature

# 4. Fusionner dans main
git checkout main
git pull origin main
git merge nouvelle-feature
git push origin main

# 5. Supprimer la branche
git branch -d nouvelle-feature
git push origin --delete nouvelle-feature

## Résolution de conflits
# Après un merge avec conflits:
# 1. Éditer les fichiers en conflit
# 2. git add fichiers-resolus
# 3. git commit (sans -m, message automatique)

## Tags (versions)
git tag v1.0.0                     # Créer un tag
git push origin v1.0.0             # Pousser un tag
git tag -d v1.0.0                  # Supprimer un tag local
git push origin --delete v1.0.0    # Supprimer un tag distant
